import { MatErrorInputDirective } from './mat-error-input.directive';

describe('MatErrorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MatErrorInputDirective();
    expect(directive).toBeTruthy();
  });
});
